const std = @import("std");

fn fibonacci(n: u32) u32 {
    if (n <= 1) return n;
    return fibonacci(n - 1) + fibonacci(n - 2);
}

pub fn main() void {
    for (i in 0..10) {
        std.debug.print("{}: {}\n", .{i, fibonacci(i)});
    }
}
