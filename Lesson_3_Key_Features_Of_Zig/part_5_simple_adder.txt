// math.zig
pub fn add(a: i32, b: i32) i32 {
    return a + b;
}

// main.zig
const std = @import("std");
const math = @import("math.zig");

pub fn main() void {
    const result = math.add(5, 7);
    std.debug.print("The result of adding is: {}\n", .{result});
}
