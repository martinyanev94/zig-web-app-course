const Builder = @import("std").build.Builder;

pub fn build(b: *Builder) void {
    const exe = b.addExecutable("my_app", "src/main.zig");
    exe.setTarget(.native);
    exe.setBuildMode(.ReleaseSmall);
    exe.install();
}
