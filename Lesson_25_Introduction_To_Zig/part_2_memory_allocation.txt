const std = @import("std");

pub fn main() !void {
    const allocator = std.heap.c_allocator;
    var buffer = try allocator.alloc(u8, 10);
    defer allocator.free(buffer);

    std.debug.print("Allocated 10 bytes of memory: {:#x}\n", .{buffer});
}
