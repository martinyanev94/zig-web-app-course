const std = @import("std");

pub fn main() !void {
    const x: i32 = 5;
    const y: i32 = 10;
    const sum: i32 = add(x, y);
    std.debug.print("The sum of {} and {} is {}.\n", .{x, y, sum});
}

fn add(a: i32, b: i32) i32 {
    return a + b;
}
