const std = @import("std");

pub fn main() !void {
    const allocator = std.heap.page_allocator;
    var buffer = try allocator.alloc(u8, 1024);
    defer allocator.free(buffer);
    
    std.debug.print("Allocated 1024 bytes of memory at: {:#x}\n", .{buffer});
}
